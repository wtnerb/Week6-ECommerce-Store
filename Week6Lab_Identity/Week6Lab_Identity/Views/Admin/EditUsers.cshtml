@model IEnumerable<ApplicationUser>
@{
    ViewData["Title"] = "View";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<table>
    <thead>
        <tr>

        </tr>
    </thead>
    <tbody>
        @foreach(ApplicationUser u in Model)
        {
        <tr class="repeated">
            <td>@u.Email</td>
            <td>@u.DateRegistered</td>
            <td>@u.UserName</td>
            <td>
                <a asp-action="DeleteUsers" asp-route-id="@u.Id">@*broken*@
                    delete
                </a>
                <a href="~/Admin/EditUser/@u.Id.ToString()">@*Match to Delete *@
                    Edit
                </a>
                <a>
                    make Admin
                </a>
            </td>
        </tr>


        }
    </tbody>
</table>
